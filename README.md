# db_manager
This simple database manager/CRM system for MSSQL. He can connect/disconnect database, showing most atributes ib db, for exemple triggers or indexces.  Also can executing base CRUD command.
